// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("wordutil", "for wordutil support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("wordutil", "enable wordutil support", "no");

if (PHP_WORDUTIL != "no") {
	EXTENSION("wordutil", "wordutil.c");
}

